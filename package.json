{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [ 
    "packages/*"
  ],
  "scripts": {
    "boots": "rm -rf ./node_modules && lerna bootstrap",
    "start:app1": "cd packages/app1 && yarn start",
    "start:app2": "cd packages/app2 && yarn start"
  }
}
